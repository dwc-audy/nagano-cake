<div class="container">
  <div class="row mb-5 mt-4">
    <div class="col-md-12"></div>
    <h4>商品詳細</h4>
  </div>

  <div class="row">
    <div class="col-md-4 text-right">
      <%= attachment_image_tag @item, :image, size: "200x200" %>
    </div>
    <div class="col-md-4">
      <p class="mb-2">商品名</p><br />
      <p class="mb-2">商品説明</p><br />
      <p class="mb-2">ジャンル</p><br />
      <p class="mb-2">税込価格（税抜価格）</p><br />
      <p>販売ステータス</p>
    </div>

    <div class="col-md-4">
      <p class="mb-2"><%= @item.name %></p><br />
      <p class="mb-2"><%= @item.introduction %></p><br />
      <p class="mb-2"><%= @item.genre.name %></p><br />
      <p class="mb-2"><%= @tax_price.to_s(:delimited) %>(<%= @item.price.to_s(:delimited) %>)円</p><br />
      <% if @item.is_active == true %>
        <p class="text-success">販売中</p>
      <% else %>
        <p class="text-danger">販売停止中</p>
      <% end %>
    </div>
  </div>

  <div class="row">
    <div class="col-md-3 mx-auto mt-5">
      <p><%= link_to '編集する', edit_admin_item_path(@item), class: "btn btn-success btn-block" %></p>
    </div>
  </div>
</div>