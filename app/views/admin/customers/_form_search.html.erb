<%= form_with url: admin_customers_search_path, method: :post, local: true do |f| %>
  <%= f.select :column, options_for_select( " 名前(姓)": "last_name", "名前(名)": "first_name") %>
  <%= f.text_field :value, placeholder: "名前(姓)" %>
  <%= f.submit "検索" %>
<% end %>